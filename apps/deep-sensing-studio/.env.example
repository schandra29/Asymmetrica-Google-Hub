# JULES-10: Deployment & Infrastructure
# Environment variable template for the Deep-Sensing Studio API.
# Copy this file to .env for local development and fill in the values.
# In production (Render.com), these are set in the dashboard or via render.yaml.

# -- Service Connections --
# These are automatically provided by Render for deployed services.
# For local development, point them to your local database and Redis instances.
DATABASE_URL="postgresql://user:password@localhost:5432/deep_sensing_db?schema=public"
REDIS_URL="redis://localhost:6379"

# -- Application Settings --
# Port for the web server to listen on.
PORT=10000

# CORS origin for the frontend application.
# Use a specific URL for production, or "*" for permissive local development.
CORS_ORIGIN="http://localhost:3000"

# -- Security --
# A strong, secret string used for signing JWTs.
# Generate a secure random string for this value.
JWT_SECRET="your-super-secret-jwt-token-that-is-at-least-32-characters-long"

# -- Node Environment --
# Set to 'development' for local work, 'production' for deployments.
NODE_ENV=development